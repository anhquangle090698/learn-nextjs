import * as React from 'react';

export interface PromotionPageProps {
}

export default function App(props: PromotionPageProps) {
    return (
        <div>
            Promotion Page
        </div>
    );
}
